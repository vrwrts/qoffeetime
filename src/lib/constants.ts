/** Lowercase name (rendered with CSS `first-letter:uppercase` in places). */
export const APP_NAME = 'qoffeetime';

/** Display title used for the document/tab title. */
export const APP_TITLE = 'Qoffeetime';
